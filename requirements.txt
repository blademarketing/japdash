# Core Flask Framework
flask==3.1.1
flask-cors==6.0.1

# HTTP Requests
requests==2.32.4

# Environment Configuration
python-dotenv==1.0.0

# Authentication
Flask-Login==0.6.3

# Database (sqlite3 is built-in with Python)
# No additional database dependencies needed

# Date/Time Handling (datetime is built-in)
# No additional datetime dependencies needed

# Logging (logging is built-in with Python) 
# No additional logging dependencies needed

# Threading (threading is built-in with Python)
# No additional threading dependencies needed

# JSON/XML Processing (json and xml.etree.ElementTree are built-in)
# No additional JSON/XML dependencies needed

# Type Hints (typing is built-in in Python 3.8+)
# No additional typing dependencies needed

# Regular Expressions (re is built-in)
# No additional regex dependencies needed