# JAP Dashboard - Environment Configuration Template
# 
# IMPORTANT: API keys and settings can be configured via the web UI.
# Use the Settings tab in the web interface for easy management.
# Changes made through the web UI take effect immediately without restart.

# API Keys (can be updated via Settings tab in web interface)
JAP_API_KEY=your_jap_api_key_here
RSS_API_KEY=your_rss_app_api_key_here
RSS_API_SECRET=your_rss_app_secret_here

# Database Configuration
DATABASE_PATH=social_media_accounts.db
JAP_CACHE_DB_PATH=jap_cache.db

# Flask Server Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5079
FLASK_DEBUG=True

# Application Settings (can be updated via Settings tab)
TIME_ZONE=Europe/London

# SETUP INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Add your API keys above OR use the web Settings tab after startup
# 3. Start the application: python app.py
# 4. Access http://localhost:5079 and use Settings tab for configuration